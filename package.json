{
  "name": "admitted",
  "version": "0.1.0",
  "private": true,
  "description": "The AI premed counselor that can't lie. Verified-data college-decision engine (core) + web PWA surface.",
  "scripts": {
    "test": "node --test \"test/**/*.test.mjs\"",
    "test:watch": "node --test --watch \"test/**/*.test.mjs\""
  }
}
